{
  "name": "@danzimon/next-best-prompt-mcp",
  "version": "0.3.0",
  "description": "MCP server for the next_best_prompt 'next-best-prompts' end-of-turn rule \u2014 end a substantive turn with 2-4 ranked, copy-paste-ready next moves, and skip when there's none. Pure guidance: no secrets, no network, no side-effecting tools. Install locally (stdio) for Claude Code / Claude Desktop. The repo's hosted endpoint (remote/) serves the newer closure_scheduler v0.5.6 rule instead; enable only one end-of-turn rule server per client.",
  "type": "module",
  "bin": {
    "next-best-prompt-mcp": "dist/index.js",
    "next-best-prompt-mcp-server": "dist/index.js"
  },
  "files": [
    "dist",
    "ALWAYS-ON.md"
  ],
  "scripts": {
    "embed": "node scripts/embed.mjs",
    "check-sync": "node scripts/check-sync.mjs",
    "check-always-on": "node scripts/check-always-on.mjs",
    "prebuild": "node scripts/embed.mjs",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "node scripts/check-sync.mjs && npm run build",
    "smoke": "node scripts/smoke.mjs",
    "test": "npm run check-sync && npm run check-always-on && npm run build && npm run smoke && npm run smoke:closure && npm run test:closure-validator && npm run test:wrapper-guard",
    "start": "node dist/index.js",
    "smoke:closure": "node scripts/smoke-closure.mjs",
    "smoke:remote": "node scripts/smoke-remote.mjs",
    "test:closure-validator": "node scripts/test-closure-validator.mjs",
    "test:wrapper-guard": "node scripts/test-wrapper-guard.mjs",
    "validate:closure": "node scripts/validate-closure-output.mjs"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "claude",
    "claude-code",
    "claude-desktop",
    "next-best-prompts",
    "next_best_prompt",
    "ai",
    "prompt"
  ],
  "author": "Dan Zimon",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
